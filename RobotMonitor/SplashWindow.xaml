<Window x:Class="RobotMonitor_3.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotMonitor_3"
        mc:Ignorable="d"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="CenterScreen"
        Title="SplashWindow" Height="350" Width="600">

    <Border Background="White" CornerRadius="10" Padding="28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0">
                <Image Source="/Resources/MRS3_Splash.png"/>
            </Border>

            <StackPanel Grid.Row="1">
                <TextBlock x:Name="StatusText"
                           Text="초기화 중..."
                           Foreground="Black" FontSize="13"
                           Margin="0,0,0,10"/>
                <ProgressBar x:Name="Bar" Height="4"
                             Minimum="0" Maximum="100" Value="0"
                             Foreground="#FF007ACC" Background="#FF3F3F46"
                             BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
